---
"pkgbld": minor
---

Discover scoped build plugins named @scope/pkgbld-plugin-* while preserving the existing unscoped naming pattern and loading each package once. Centralize PKG BLD's name predicate and keep create-pkgbld's discovery behavior covered by parity tests.
